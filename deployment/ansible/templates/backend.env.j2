ENVIRONMENT={{ environment | default("production") }}
DATABASE_URL={{ database_url }}
REDIS_URL={{ redis_url }}
HOTSPOT_INTERFACE={{ hotspot_interface | default("wlan0") }}
MEETING_THRESHOLD={{ meeting_threshold | default(3) }}
MEETING_WINDOW_MINUTES={{ meeting_window_minutes | default(5) }}
POLL_INTERVAL_SECONDS={{ poll_interval_seconds | default(10) }}
HISTORY_LIMIT={{ history_limit | default(12) }}
POLLING_ENABLED={{ polling_enabled | default(true) | lower }}

